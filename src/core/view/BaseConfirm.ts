class BaseConfirm extends BaseView {
	public constructor() {
		super();
		this.skinName = "confirm_skin";
	}

	public static show(msg: string, btnLabels: string[], callBackIds: number[], callBack: Function, thisObj: any){
		
	}
}